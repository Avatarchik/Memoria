using UnityEngine;
using System.Collections;


public class ActionMgr : MonoBehaviour {
    /*
    public static ActionMgr instance = new ActionMgr();

    public delegate void AttackTarget(Entity e, Damage d);
    public delegate void TakeDamage(Damage d);
    public delegate void UseSkill(Entity e);
    */
    // Use this for initialization
	void Start () {

	}

	// Update is called once per frame
	void Update () {

	}
}
